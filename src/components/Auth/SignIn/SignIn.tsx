import React, { FC } from 'react';
import { SignInProps } from './SignIn.types';
import './SignIn.css';

const SignUp: FC<SignInProps> = (props) => {
    return (
        <div className="SignIn">
            SignIn
        </div>
    );
};

export default SignUp;
